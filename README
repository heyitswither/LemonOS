LemonOS
=======

LemonOS is an operating system written in C that I made just to figure out how an OS works.

Compiling
=========

`make tools` to create the toolchain
`make` to compile the kernel and create an ISO
`make test` to boot the kernel in QEMU
`make test-iso` to boot the ISO in QEMU
